{
  "name": "train-ticket-graph",
  "private": true,
  "scripts": {
    "setup": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -k -n api,web -c cyan,magenta \"npm run start:dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "test": "npm test --prefix backend && npm run test:e2e --prefix backend && npm test --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
